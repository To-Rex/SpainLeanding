---
import Layout from '../../layouts/Layout.astro';
import Navigation from '../../components/Navigation.astro';
import { getLangFromUrl, useTranslations, getLocalizedPath } from '../../i18n/utils';
import ContactForm from '../../components/ContactForm.astro';

export function getStaticPaths() {
  return [
    {params: {lang: undefined}},
    {params: {lang: 'uz'}},
    {params: {lang: 'en'}},
    {params: {lang: 'ru'}}
  ];
}

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Layout title={t('contact.title')} description={t('contact.subtitle')}>
	<Navigation />
	
	<section class="section hero-contact">
		<div class="container">
			<div class="section-content">
				<h1>{t('contact.title')}</h1>
				<p class="lead">{t('contact.subtitle')}</p>
			</div>
		</div>
	</section>

	<section class="section contact-options">
		<div class="container">
			<div class="section-content">
				<div class="contact-grid">
					<div class="contact-card">
						<div class="contact-icon">📧</div>
						<h3>{t('contact.email.title')}</h3>
						<p>{t('contact.email.desc')}</p>
						<a href="mailto:umidyusupov258@gmail.com" class="contact-link">umidyusupov258@gmail.com</a>
						<small>{t('contact.email.response')}</small>
					</div>
					
					<div class="contact-card">
						<div class="contact-icon">📱</div>
						<h3>{t('contact.whatsapp.title')}</h3>
						<p>{t('contact.whatsapp.desc')}</p>
						<a href="https://wa.me/998910216001" class="contact-link" target="_blank">+998 91 021 60 01</a>
						<small>{t('contact.whatsapp.hours')}</small>
					</div>
					
					<div class="contact-card">
						<div class="contact-icon">✈️</div>
						<h3>{t('contact.telegram.title')}</h3>
						<p>{t('contact.telegram.desc')}</p>
						<a href="https://t.me/+998910216001" class="contact-link" target="_blank">+998 91 021 60 01</a>
						<small>{t('contact.telegram.support')}</small>
					</div>
				</div>
			</div>
		</div>
	</section>

	<section class="section contact-form">
		<div class="container">
			<div class="section-content">
				<div class="form-container">
					<div class="form-header">
						<h2>{t('contact.form.title')}</h2>
						<p>{t('contact.form.desc')}</p>
					</div>
					
					<ContactForm lang={lang} />
				</div>
			</div>
		</div>
	</section>

	<section class="section services">
		<div class="container">
			<div class="section-content">
				<h2>{t('contact.services.title')}</h2>
				<div class="services-grid">
					<div class="service-card">
						<div class="service-icon">🗺️</div>
						<h3>{t('contact.services.itineraries.title')}</h3>
						<p>{t('contact.services.itineraries.desc')}</p>
					</div>
					
					<div class="service-card">
						<div class="service-icon">🏨</div>
						<h3>{t('contact.services.accommodation.title')}</h3>
						<p>{t('contact.services.accommodation.desc')}</p>
					</div>
					
					<div class="service-card">
						<div class="service-icon">🚗</div>
						<h3>{t('contact.services.transport.title')}</h3>
						<p>{t('contact.services.transport.desc')}</p>
					</div>
					
					<div class="service-card">
						<div class="service-icon">👥</div>
						<h3>{t('contact.services.cultural.title')}</h3>
						<p>{t('contact.services.cultural.desc')}</p>
					</div>
					
					<div class="service-card">
						<div class="service-icon">📋</div>
						<h3>{t('contact.services.visa.title')}</h3>
						<p>{t('contact.services.visa.desc')}</p>
					</div>
					
					<div class="service-card">
						<div class="service-icon">🎯</div>
						<h3>{t('contact.services.consulting.title')}</h3>
						<p>{t('contact.services.consulting.desc')}</p>
					</div>
				</div>
			</div>
		</div>
	</section>

	<section class="section faq">
		<div class="container">
			<div class="section-content">
				<h2>{t('contact.faq.title')}</h2>
				<div class="faq-grid">
					<div class="faq-item">
						<h3>{t('contact.faq.cost.q')}</h3>
						<p>{t('contact.faq.cost.a')}</p>
					</div>
					
					<div class="faq-item">
						<h3>{t('contact.faq.season.q')}</h3>
						<p>{t('contact.faq.season.a')}</p>
					</div>
					
					<div class="faq-item">
						<h3>{t('contact.faq.visa_faq.q')}</h3>
						<p>{t('contact.faq.visa_faq.a')}</p>
					</div>
					
					<div class="faq-item">
						<h3>{t('contact.faq.safety.q')}</h3>
						<p>{t('contact.faq.safety.a')}</p>
					</div>
					
					<div class="faq-item">
						<h3>{t('contact.faq.language.q')}</h3>
						<p>{t('contact.faq.language.a')}</p>
					</div>
					
					<div class="faq-item">
						<h3>{t('contact.faq.duration.q')}</h3>
						<p>{t('contact.faq.duration.a')}</p>
					</div>
				</div>
				
				<a href={getLocalizedPath('/', lang)} class="back-to-top">{t('back.home')}</a>
			</div>
		</div>
	</section>
</Layout>

<style>
	.hero-contact {
		background: linear-gradient(135deg, #764BA2 0%, #667EEA 100%);
		color: white;
		text-align: center;
		padding: 8rem 0 5rem;
	}

	.hero-contact h1 {
		font-size: 3rem;
		margin-bottom: 1rem;
		color: white;
	}

	.lead {
		font-size: 1.3rem;
		color: rgba(255, 255, 255, 0.9);
		max-width: 600px;
		margin: 0 auto;
	}

	.contact-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		margin-top: 3rem;
	}

	.contact-card {
		background: white;
		padding: 2.5rem;
		border-radius: 16px;
		box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
		text-align: center;
		transition: transform 0.3s ease;
	}

	.contact-card:hover {
		transform: translateY(-5px);
	}

	.contact-icon {
		font-size: 3rem;
		margin-bottom: 1.5rem;
	}

	.contact-card h3 {
		color: #1A365D;
		margin-bottom: 1rem;
		font-size: 1.3rem;
	}

	.contact-card p {
		color: #4A5568;
		margin-bottom: 1.5rem;
	}

	.contact-link {
		display: inline-block;
		color: #D4AF37;
		text-decoration: none;
		font-weight: 500;
		font-size: 1.1rem;
		margin-bottom: 0.5rem;
		transition: color 0.3s ease;
	}

	.contact-link:hover {
		color: #B8860B;
	}

	.contact-card small {
		color: #718096;
		font-size: 0.9rem;
	}

	.contact-form {
		background: #F8F9FA;
		padding: 6rem 0;
	}

	.form-container {
		max-width: 800px;
		margin: 0 auto;
		background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(248, 250, 252, 0.95) 100%);
		backdrop-filter: blur(20px);
		-webkit-backdrop-filter: blur(20px);
		padding: 4rem;
		border-radius: 24px;
		box-shadow: 
			0 20px 60px rgba(0, 0, 0, 0.15),
			0 0 0 1px rgba(212, 175, 55, 0.1),
			inset 0 1px 0 rgba(255, 255, 255, 0.8);
		border: 1px solid rgba(255, 255, 255, 0.2);
		position: relative;
		overflow: hidden;
	}

	.form-container::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 200%;
		height: 100%;
		background: linear-gradient(90deg, 
			transparent 0%, 
			rgba(212, 175, 55, 0.05) 50%, 
			transparent 100%);
		animation: shimmer 4s ease-in-out infinite;
	}

	.form-header {
		text-align: center;
		margin-bottom: 4rem;
		position: relative;
		z-index: 2;
	}

	.form-header h2 {
		background: linear-gradient(135deg, #1A365D 0%, #2C5282 50%, #D4AF37 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin-bottom: 1.5rem;
		font-size: 2.5rem;
		font-weight: 700;
		text-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
		position: relative;
	}

	.form-header h2::after {
		content: '';
		position: absolute;
		bottom: -10px;
		left: 50%;
		transform: translateX(-50%);
		width: 80px;
		height: 4px;
		background: linear-gradient(90deg, #D4AF37, #F7DC6F, #D4AF37);
		border-radius: 2px;
		box-shadow: 0 2px 10px rgba(212, 175, 55, 0.3);
	}

	.form-header p {
		color: #4A5568;
		font-size: 1.2rem;
		line-height: 1.6;
		max-width: 500px;
		margin: 0 auto;
		font-weight: 400;
	}

	.form-row {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 2rem;
		position: relative;
		z-index: 2;
	}

	.form-group {
		margin-bottom: 2rem;
		position: relative;
		z-index: 2;
	}

	.form-group label {
		display: block;
		color: #1A365D;
		margin-bottom: 0.8rem;
		font-weight: 600;
		font-size: 1rem;
		text-transform: uppercase;
		letter-spacing: 0.5px;
		position: relative;
	}

	.form-group label::before {
		content: '';
		position: absolute;
		left: -15px;
		top: 50%;
		transform: translateY(-50%);
		width: 4px;
		height: 4px;
		background: #D4AF37;
		border-radius: 50%;
		box-shadow: 0 0 10px rgba(212, 175, 55, 0.5);
	}

	.form-group input,
	.form-group select,
	.form-group textarea {
		width: 100%;
		padding: 1rem 1.5rem;
		border: 2px solid rgba(226, 232, 240, 0.8);
		border-radius: 12px;
		font-size: 1.05rem;
		background: rgba(255, 255, 255, 0.9);
		backdrop-filter: blur(10px);
		transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
		box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
		font-family: inherit;
	}

	.form-group input:focus,
	.form-group select:focus,
	.form-group textarea:focus {
		outline: none;
		border-color: rgba(212, 175, 55, 0.8);
		background: rgba(255, 255, 255, 1);
		box-shadow: 
			0 8px 25px rgba(0, 0, 0, 0.1),
			0 0 0 4px rgba(212, 175, 55, 0.1);
		transform: translateY(-2px);
	}

	.form-group textarea {
		resize: vertical;
		min-height: 120px;
		line-height: 1.6;
	}

	.checkbox-group {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
		gap: 1.5rem;
		margin-top: 1rem;
		padding: 1.5rem;
		background: rgba(248, 250, 252, 0.8);
		border-radius: 12px;
		border: 1px solid rgba(212, 175, 55, 0.1);
	}

	.checkbox-label {
		display: flex;
		align-items: center;
		color: #2C5282;
		cursor: pointer;
		font-weight: 500;
		padding: 0.8rem;
		border-radius: 8px;
		transition: all 0.3s ease;
		position: relative;
	}

	.checkbox-label:hover {
		background: rgba(212, 175, 55, 0.1);
		transform: translateX(5px);
	}

	.checkbox-label input[type="checkbox"] {
		width: auto;
		margin-right: 1rem;
		transform: scale(1.2);
		accent-color: #D4AF37;
	}

	.btn-primary {
		background: linear-gradient(135deg, #D4AF37 0%, #F7DC6F 50%, #D4AF37 100%);
		color: #1A365D;
		padding: 1.2rem 3rem;
		border: none;
		border-radius: 16px;
		font-size: 1.2rem;
		font-weight: 700;
		cursor: pointer;
		transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
		width: 100%;
		text-transform: uppercase;
		letter-spacing: 1px;
		position: relative;
		overflow: hidden;
		box-shadow: 
			0 8px 25px rgba(212, 175, 55, 0.3),
			inset 0 1px 0 rgba(255, 255, 255, 0.3);
		z-index: 2;
	}

	.btn-primary::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, 
			transparent 0%, 
			rgba(255, 255, 255, 0.3) 50%, 
			transparent 100%);
		transition: all 0.6s ease;
	}

	.btn-primary:hover {
		transform: translateY(-4px);
		box-shadow: 
			0 15px 40px rgba(212, 175, 55, 0.4),
			inset 0 1px 0 rgba(255, 255, 255, 0.4);
		background: linear-gradient(135deg, #F7DC6F 0%, #D4AF37 50%, #F7DC6F 100%);
	}

	.btn-primary:hover::before {
		left: 100%;
	}

	.btn-primary:active {
		transform: translateY(-2px);
	}

	/* Success and error message styles */
	.form-message {
		padding: 1.5rem 2rem;
		border-radius: 12px;
		margin-bottom: 2rem;
		font-weight: 600;
		font-size: 1.05rem;
		position: relative;
		z-index: 2;
		backdrop-filter: blur(10px);
		-webkit-backdrop-filter: blur(10px);
	}

	.form-message.success {
		background: linear-gradient(135deg, rgba(240, 253, 244, 0.95) 0%, rgba(187, 247, 208, 0.95) 100%);
		color: #065F46;
		border: 2px solid rgba(34, 197, 94, 0.3);
		box-shadow: 0 8px 25px rgba(34, 197, 94, 0.15);
	}

	.form-message.error {
		background: linear-gradient(135deg, rgba(254, 242, 242, 0.95) 0%, rgba(254, 202, 202, 0.95) 100%);
		color: #991B1B;
		border: 2px solid rgba(239, 68, 68, 0.3);
		box-shadow: 0 8px 25px rgba(239, 68, 68, 0.15);
	}

	.form-message.loading {
		background: linear-gradient(135deg, rgba(239, 246, 255, 0.95) 0%, rgba(219, 234, 254, 0.95) 100%);
		color: #1E40AF;
		border: 2px solid rgba(59, 130, 246, 0.3);
		box-shadow: 0 8px 25px rgba(59, 130, 246, 0.15);
	}

	/* Animations */
	@keyframes shimmer {
		0% { transform: translateX(-100%); }
		100% { transform: translateX(100%); }
	}

	@keyframes fadeInUp {
		from {
			opacity: 0;
			transform: translateY(30px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	.form-group {
		animation: fadeInUp 0.6s ease-out forwards;
	}

	.form-group:nth-child(1) { animation-delay: 0.1s; }
	.form-group:nth-child(2) { animation-delay: 0.2s; }
	.form-group:nth-child(3) { animation-delay: 0.3s; }
	.form-group:nth-child(4) { animation-delay: 0.4s; }
	.form-group:nth-child(5) { animation-delay: 0.5s; }
	.form-group:nth-child(6) { animation-delay: 0.6s; }
	}

	.form-row {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1.5rem;
	}

	.form-group {
		margin-bottom: 1.5rem;
	}

	.form-group label {
		display: block;
		color: #2C5282;
		margin-bottom: 0.5rem;
		font-weight: 500;
	}

	.form-group input,
	.form-group select,
	.form-group textarea {
		width: 100%;
		padding: 0.75rem;
		border: 2px solid #E2E8F0;
		border-radius: 8px;
		font-size: 1rem;
		transition: border-color 0.3s ease;
	}

	.form-group input:focus,
	.form-group select:focus,
	.form-group textarea:focus {
		outline: none;
		border-color: #D4AF37;
	}

	.checkbox-group {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: 1rem;
		margin-top: 0.5rem;
	}

	.checkbox-label {
		display: flex;
		align-items: center;
		color: #4A5568;
		cursor: pointer;
		font-weight: normal;
	}

	.checkbox-label input[type="checkbox"] {
		width: auto;
		margin-right: 0.5rem;
	}

	.btn-primary {
		background: linear-gradient(135deg, #D4AF37 0%, #F7DC6F 100%);
		color: #1A365D;
		padding: 1rem 2rem;
		border: none;
		border-radius: 8px;
		font-size: 1.1rem;
		font-weight: 500;
		cursor: pointer;
		transition: all 0.3s ease;
		width: 100%;
	}

	.btn-primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 6px 20px rgba(212, 175, 55, 0.3);
	}

	/* Success and error message styles */
	.form-message {
		padding: 1rem;
		border-radius: 8px;
		margin-bottom: 1rem;
		font-weight: 500;
	}

	.form-message.success {
		background: #F0FDF4;
		color: #166534;
		border: 1px solid #BBF7D0;
	}

	.form-message.error {
		background: #FEF2F2;
		color: #DC2626;
		border: 1px solid #FECACA;
	}

	.form-message.loading {
		background: #EFF6FF;
		color: #1D4ED8;
		border: 1px solid #DBEAFE;
	}

	.services-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		margin-top: 3rem;
	}

	.service-card {
		background: white;
		padding: 2rem;
		border-radius: 12px;
		box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
		text-align: center;
		transition: transform 0.3s ease;
	}

	.service-card:hover {
		transform: translateY(-3px);
	}

	.service-icon {
		font-size: 2.5rem;
		margin-bottom: 1rem;
	}

	.service-card h3 {
		color: #2C5282;
		margin-bottom: 1rem;
		font-size: 1.2rem;
	}

	.service-card p {
		color: #4A5568;
		line-height: 1.6;
	}

	.faq {
		background: #F8F9FA;
		padding: 6rem 0;
	}

	.faq-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
		gap: 2rem;
		margin-top: 3rem;
	}

	.faq-item {
		background: white;
		padding: 2rem;
		border-radius: 12px;
		box-shadow: 0 8px 32px rgba(0, 0, 0, 0.05);
		border-left: 4px solid #D4AF37;
	}

	.faq-item h3 {
		color: #1A365D;
		margin-bottom: 1rem;
		font-size: 1.2rem;
	}

	.faq-item p {
		color: #4A5568;
		line-height: 1.6;
		margin: 0;
	}

	@media (max-width: 768px) {
		.contact-grid {
			grid-template-columns: 1fr;
		}

		.form-row {
			grid-template-columns: 1fr;
		}

		.form-container {
			padding: 2rem;
		}

		.checkbox-group {
			grid-template-columns: 1fr;
		}

		.services-grid {
			grid-template-columns: 1fr;
		}

		.faq-grid {
			grid-template-columns: 1fr;
		}

		.form-row {
			grid-template-columns: 1fr;
			gap: 1rem;
		}

		.form-container {
			padding: 2.5rem 2rem;
			margin: 0 1rem;
		}

		.form-header h2 {
			font-size: 2rem;
		}

		.checkbox-group {
			grid-template-columns: 1fr;
			gap: 1rem;
		}

		.btn-primary {
			padding: 1rem 2rem;
			font-size: 1.1rem;
		}

		.hero-contact h1 {
			font-size: 2.5rem;
		}
	}

	@media (max-width: 480px) {
		.form-container {
			padding: 2rem 1.5rem;
			margin: 0 0.5rem;
		}

		.form-header h2 {
			font-size: 1.8rem;
		}

		.form-group input,
		.form-group select,
		.form-group textarea {
			padding: 0.8rem 1rem;
			font-size: 1rem;
		}
	}
</style>